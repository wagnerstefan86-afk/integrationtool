# Harmonizer Deployment Configuration
# Copy to .env and adjust as needed

# Environment
APP_ENV=production

# Ports
BACKEND_PORT=8001
FRONTEND_PORT=3001

# Host paths for persistent data (mounted into containers)
DATA_PATH=./deploy/data
REPORT_PATH=./deploy/reports
LOG_PATH=./deploy/logs
